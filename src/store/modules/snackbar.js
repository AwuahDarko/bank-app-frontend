

const state = {
    
}


const getters = {
    

}

const actions = {
    
    showSnackbar({ commit }, details){
        commit('displaySnackbar', details)
    }
}

const mutations = {
    displaySnackbar: (state, details) => {
        state;
        details;
        
    }
}

export default {
    state,
    getters,
    actions,
    mutations
}